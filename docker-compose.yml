# FastAPI Transcript API – tek servis
# Kullanım: docker-compose up --build -d
# Loglar: docker-compose logs -f
services:
  youtube-api:
    build: .
    container_name: youtube-api
    ports:
      - "8000:8000"
    restart: unless-stopped
