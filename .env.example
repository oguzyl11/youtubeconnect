# ========== Sunucu (production) ==========
# Sunucuda mutlaka: SECRET_KEY, ALLOWED_HOSTS, BASE_URL gerçek değerlerle doldurulmalı.
# Nginx/Caddy arkasında HTTPS kullanıyorsanız X-Forwarded-Proto: https gönderin.

# Django
SECRET_KEY=your-secret-key-min-50-chars-use-openssl-rand-hex-32
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,.yourdomain.com

# Site URL – sunucuda https://alanadiniz.com gibi (sonunda / olmasın)
BASE_URL=http://localhost:8000

# Docker / sunucu (opsiyonel)
# PORT=8000
# GUNICORN_WORKERS=3

# Lokal (dil ve saat dilimi)
LANGUAGE_CODE=tr-tr
TIME_ZONE=Europe/Istanbul

# Docker Compose - PostgreSQL
POSTGRES_DB=youtubeconnect
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Database (production: PostgreSQL - docker-compose ile db servisi kullanılıyor)
# DATABASE_URL web servisinde otomatik set edilir
# Local / no DB_URL = SQLite kullanılır

# CORS & CSRF (production'da domain'lerinizi ekleyin)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Transkript: ScrapingBee (öncelikli, engelden kaçınma) -> Playwright -> youtube-transcript-api
# ScrapingBee API key: https://www.scrapingbee.com/ (ücretsiz kredi ile deneyebilirsiniz)
SCRAPINGBEE_API_KEY=
# TRANSCRIPT_USE_SCRAPINGBEE=true
# Browser scraper (ScrapingBee yoksa): TRANSCRIPT_USE_BROWSER_SCRAPER=true
# TRANSCRIPT_BROWSER_TIMEOUT=30000
# TRANSCRIPT_BROWSER_HEADLESS=true
# Proxy (sadece browser/API için): YOUTUBE_PROXY=http://user:pass@host:port

# Optional: settings module (development/production)
# DJANGO_SETTINGS_MODULE=config.settings.development
# DJANGO_SETTINGS_MODULE=config.settings.production
